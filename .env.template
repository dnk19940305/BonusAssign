# 奖金系统环境变量配置模板
# 复制此文件为 .env 并根据实际情况修改配置

# ========================================
# 应用基础配置
# ========================================
NODE_ENV=development
# NODE_ENV=production

# ========================================
# 端口配置
# ========================================
# 前端端口
FRONTEND_PORT=8080

# 后端API端口
BACKEND_PORT=3000

# MySQL数据库端口（外部 MySQL 服务器端口）
DB_PORT=3306

# MySQL 主机地址（外部 MySQL 服务器 IP 或域名）
DB_HOST=localhost

# Redis端口（已禁用）
# REDIS_PORT=6379

# Adminer数据库管理工具端口（已移除，使用外部 MySQL）
# ADMINER_PORT=8081

# ========================================
# MySQL 数据库配置（外部 MySQL 服务器）
# ========================================
# MySQL 数据库名
DB_NAME=bonus_system

# MySQL 用户名
DB_USER=root

# MySQL 用户密码（生产环境必须修改！）
DB_PASSWORD=rootpassowrd

# 注意：不再需要以下配置（使用外部 MySQL）
# MYSQL_ROOT_PASSWORD=root_password
# MYSQL_DATABASE=bonus_system
# MYSQL_USER=bonus_user
# MYSQL_PASSWORD=bonus_pass

# ========================================
# Redis 配置（已禁用）
# ========================================
# Redis 已禁用，不需要配置
# REDIS_PASSWORD=redis_pass

# ========================================
# JWT 认证配置
# ========================================
# JWT 密钥（生产环境必须修改为随机字符串！）
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# JWT 刷新令牌密钥（生产环境必须修改为随机字符串！）
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production

# JWT 过期时间
JWT_EXPIRES_IN=2h

# JWT 刷新令牌过期时间
JWT_REFRESH_EXPIRES_IN=7d

# ========================================
# 日志配置
# ========================================
# 日志级别: debug, info, warn, error
LOG_LEVEL=info

# ========================================
# 安全配置
# ========================================
# CORS 跨域配置
CORS_ORIGIN=http://localhost

# 速率限制窗口（分钟）
RATE_LIMIT_WINDOW=15

# 速率限制最大请求数
RATE_LIMIT_MAX=100

# ========================================
# Grafana 配置（仅生产环境）
# ========================================
# Grafana 管理员密码（可选）
GRAFANA_PASSWORD=admin123

# Grafana 访问端口（生产环境监控）
# 访问地址: http://localhost:3001
# 注意：Grafana 使用 3001 端口，避免与后端 3000 端口冲突
GRAFANA_PORT=3001

# ========================================
# 其他配置
# ========================================
# Docker Volume 模式
VOLUME_MODE=cached

# 后端 URL（用于前端构建，使用 localhost）
BACKEND_URL=http://localhost:3000

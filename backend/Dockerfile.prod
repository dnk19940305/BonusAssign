# 后端生产环境 Dockerfile - 简化版（使用 root 用户）
FROM node:22.18.0-alpine

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 配置npm淘宝镜像源并安装生产依赖（跳过可选依赖以加快安装速度）
RUN npm config set registry https://registry.npmmirror.com && \
    npm install --production --no-optional && \
    npm cache clean --force

# 复制应用代码
COPY . .

# 创建必要目录
RUN mkdir -p logs database

# 暴露端口
EXPOSE 3000

# 生产模式启动
CMD ["node", "src/app.js"]